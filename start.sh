#!/bin/bash
# Start both Flask backend and Streamlit frontend in parallel

# Start Flask backend in background
echo "🚀 Starting Flask backend..."
 # Start only Flask
 echo "🚀 Starting Flask backend..."
 python main.py
